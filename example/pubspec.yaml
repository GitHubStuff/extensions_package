name: xample
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.0.1+4

environment:
  sdk: ">=2.15.1 <3.0.0" #Dart SDK 2021-12-20
  flutter: ">=2.8.1"

dependencies:
  cupertino_icons: ^1.0.4
  extensions_package:
    path: ../
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.1
  flutter_localizations:
    sdk: flutter
  flutter_modular: ^4.3.0
  mock_theme_management:
    git:
      url: https://github.com/RAE-Health/mock_theme_management.git

dependency_overrides: null

dev_dependencies:
  build_runner:
  flutter_test:
    sdk: flutter
  test:

flutter:
  uses-material-design: true
  generate: false
  assets:
    - assets/sample.png
